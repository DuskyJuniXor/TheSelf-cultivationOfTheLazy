name: action_dev
on: 
  push:
    branches:
      - dev
jobs:
  myjob:
      runs-on: ubuntu-20.04
      steps:
        - name: install_gcc
          run: |
            sudo apt-get update
            sudo apt-get install gcc
        - name: automation
          run: |
            git config --global user.name "DuskyJuniXor"
            git config --global user.password "DY-20050206-dkj"
            git config --global user.email "1171686724@qq.com"
            git init
            git clone https://github.com/DuskyJuniXor/TheSelf-cultivationOfTheLazy.git
            git remote add origin https://github.com/DuskyJuniXor/TheSelf-cultivationOfTheLazy.git
            cd /home/runner/work/TheSelf-cultivationOfTheLazy/TheSelf-cultivationOfTheLazy
            git fetch origin dev
            git fetch origin master
            git switch origin/master
            git merge origin/dev
            git push -u origin master
